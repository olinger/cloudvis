class ColorSet
{
  int size;
  color[] colors;// = new color[size];
  ColorSet(int size, color base)
  {
    colorMode(HSB, 360, 100, size + 10);
    colors = new color[size];
    for(int i = 0; i < size; i++)
    {
      
    }
  }
}